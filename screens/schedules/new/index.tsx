import {Text, View} from "react-native";
import styles from "./styles";

export const ScheduleNewScreen = () => {

    return (
        <View style={styles.container}>
            <Text>{''}</Text>
        </View>
    );
}

export default ScheduleNewScreen;